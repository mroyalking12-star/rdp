name: Win11-CloudDrive
on: workflow_dispatch

jobs:
  summon-pc:
    runs-on: windows-latest

    steps:
      - name: Install Tailscale
        run: |
          Invoke-WebRequest https://pkgs.tailscale.com/stable/tailscale-setup-1.82.0-amd64.msi -OutFile tailscale.msi
          Start-Process msiexec.exe -ArgumentList "/i tailscale.msi /quiet /norestart" -Wait

      - name: Enable Remote Desktop
        run: |
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' -Name "fDenyTSConnections" -Value 0
          Enable-NetFirewallRule -DisplayGroup "Remote Desktop"
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' -Name "UserAuthentication" -Value 1

      - name: Install Rclone & WinFsp
        run: |
          # WinFsp is required for the OS to see the cloud mount as a drive
          choco install winfsp rclone -y

      - name: Connect Tailscale
        run: |
          $user = "${{ github.actor }}"
          $date = (Get-Date).ToString("yyyyMMdd").ToUpper()
          $hostname = "$user-$date"
          $authKey = "tskey-auth-kimGNQPCkz11CNTRL-3fDxcDxBubijSqRCoUw4cidnqU7haxvY"
          & "C:\Program Files\Tailscale\tailscale.exe" up --authkey=$authKey --hostname=$hostname --unattended --reset

      - name: Configure User
        run: |
          $Password = ConvertTo-SecureString "MySecurePass123!" -AsPlainText -Force
          $newUser = New-LocalUser -Name "RDPUser" -Password $Password -FullName "RDP User" -Description "User for RDP"
          Add-LocalGroupMember -Group "Administrators" -Member "RDPUser"
          Add-LocalGroupMember -Group "Remote Desktop Users" -Member "RDPUser"

      - name: Mount InfiniCLOUD Drive
        shell: pwsh
        run: |
          $ConfigPath = "$env:USERPROFILE\.config\rclone\rclone.conf"
          if (!(Test-Path (Split-Path $ConfigPath))) { New-Item -ItemType Directory -Force -Path (Split-Path $ConfigPath) }

          # Obscuring the password so Rclone accepts it in the config file
          $pass = "uwWfVg3HNyQ4NE5s"
          $obscured = rclone obscure $pass

          Set-Content -Path $ConfigPath -Value @"
          [MyCloud]
          type = webdav
          url = https://otaru.infini-cloud.net/dav/
          vendor = other
          user = meownsoor
          pass = $obscured
          "@

          # Mount as S: drive. vfs-cache-mode full makes it behave like a real hard drive
          Start-Process rclone -ArgumentList "mount MyCloud: S: --vfs-cache-mode full" -WindowStyle Hidden
          
          # Small delay to let the mount initialize
          Start-Sleep -Seconds 5

      - name: Ready to Connect
        run: |
          $tsIP = & "C:\Program Files\Tailscale\tailscale.exe" ip -4
          echo "=========================================="
          echo "SUCCESS! CONNECT TO THIS IP: $tsIP"
          echo "USERNAME: RDPUser"
          echo "PASSWORD: MySecurePass123!"
          echo "CLOUD DRIVE: S:\ (InfiniCLOUD)"
          echo "=========================================="
          while($true) { Start-Sleep -Seconds 60 }
